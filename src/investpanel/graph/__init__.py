"""Graph package — skeleton only for now.

The LangGraph state, workflow, and routing that wire the agents together are
built in Phase 4. This file just reserves the package so the structure matches
the spec from Phase 0.
"""
