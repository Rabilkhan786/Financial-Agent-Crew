# Core agent crew
langgraph
langchain-core
langchain-groq
pydantic          # orchestrator.py imports this directly for ReviewDecision

# Data + maths (all arithmetic happens here, never in the LLM)
pandas
numpy
yfinance

# The API (runs the crew) and the Streamlit app that calls it over HTTP
fastapi
uvicorn
requests

# Output
matplotlib
fpdf2
streamlit

# Config + tests
python-dotenv
pytest
